<section class="rbp-section">
  <div class="rbp-container">
    {% render 'rbp-section-heading', eyebrow: section.settings.eyebrow, heading: section.settings.heading, text: section.settings.text %}
    <div class="rbp-router">
      {%- for block in section.blocks -%}
        <div {{ block.shopify_attributes }}>
          {% render 'rbp-service-card',
            title: block.settings.title,
            text: block.settings.text,
            url: block.settings.url,
            button_label: block.settings.button_label,
            icon: block.settings.icon
          %}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "RBP service router",
  "tag": "section",
  "class": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Choose a pathway" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Start with the kind of support you need" },
    { "type": "richtext", "id": "text", "label": "Text", "default": "<p>Route visitors into advisory, implementation, operations, marketplace or resource journeys.</p>" }
  ],
  "blocks": [
    {
      "type": "service",
      "name": "Service card",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon text", "default": "01" },
        { "type": "text", "id": "title", "label": "Title", "default": "Business advisory" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "Clarify decisions, risks and next steps before committing budget." },
        { "type": "text", "id": "button_label", "label": "Button label", "default": "View pathway" },
        { "type": "url", "id": "url", "label": "URL" }
      ]
    }
  ],
  "presets": [
    {
      "name": "RBP service router",
      "blocks": [
        { "type": "service" },
        { "type": "service", "settings": { "icon": "02", "title": "Implementation", "text": "Turn approved work into practical execution and delivery checkpoints." } },
        { "type": "service", "settings": { "icon": "03", "title": "Operations", "text": "Keep the business moving with support, templates and managed follow-through." } }
      ]
    }
  ]
}
{% endschema %}
